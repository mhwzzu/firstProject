spring:
  datasource:
    url: jdbc:h2:mem:journey-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;DATABASE_TO_LOWER=TRUE
  jpa:
    hibernate:
      ddl-auto: create-drop

discovery:
  enabled: false

amap:
  key:
